struct lol* topkek(void* lol, int kek, char* yous, struct memes* memes){
//vardecl
struct xd* lol;
struct topkek pizdec[9];
int* okay[2];
void* LUL[92345234];
char meap;
//stmt while
while (*-exp.exp == -*2)
return true;
//stmt if
if (a == ol){
int a;
int c;
a=9;
(4+5);
return null;
} else {
return 1;
}
//stmt return
return okay;
return;
//stmt exp = exp
me = you;
// (exp)
((((lul))));
// -exp
-"lol";
// *exp
*2;
// size of
sizeof(void);
// type cast
(int) -2;
// fieldaccess
'l'.s;
// array access
value[*--(((asd)))];
// funcall
ayy(*nice,-function,*-you,(((got))), "HERE BOI",'!',11,dont[youthink]);
// something really long that should still work somehow
*(char*)(-(*(-ayy["lmao"]))) -------'l'[sizeof(struct ints*)] +- (char)(int)(struct top*)(void)(why.topkek);
}